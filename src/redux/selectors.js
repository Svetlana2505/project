export const selectUsers = (state) => state.users.users;
export const selectTotalPage = (state) => state.users.totalPages;

export const selectPositions = (state) => state.positions.positions;
export const selectSuccess = (state) => state.registration.success;
